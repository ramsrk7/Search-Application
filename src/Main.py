from ProcessingFiles import PreprocessFiles
from GraphDB import File, Keyword, Nodes


"""

While True:
    Read Files properties
    if file not in Graph,
        add it to graph
    if files in graph not found in path, 
        delete file in graph
    
"""